import { makeStyles } from '@material-ui/styles';

export default makeStyles(theme => ({
  dialogContent: {
    paddingBottom: '16px',
  },

  button: {
    background: '#5cb895 !important',
    height: '40px',
    borderRadius: '8px',
    // marginBottom: '8px',
  },
}));
